---
description: 零偏差执行与原子审计循环协议。
globs: src/**/*, **/*.md, **/*.(ts|js|vue|css|html)
alwaysApply: false
---

# 🔩 阶段 2：原子执行铁锁 (Execution Iron Lock)

## 📜 执行使命
- 按照已获准的 `implementation_plan.md` 进行 **原子级任务递进**。
- **人类审计环**：严禁在未经过审计闸门的情况下提交大流量改动。

## 🛡️ 1. 原子审计循环 (不可跳过)
1.  **计划读取**：AI 必须在开始 1.1 任务前 `view_file` 确认计划内容。
2.  **原子步进 (Atomic Increment)** — 双轨制：
    - **逻辑变更 (JS/TS/Logic)**：严格 ≤ **20 行**；**或**一个完整的语义单元（需在 diff 前注明理由）。
    - **UI/模板 (HTML/JSX/Vue)**：≤ **100 行** **且** ≤ 1 个功能块（两个条件须同时满足）。
    - **纯重构**（无行为改变）：逻辑可放宽至 ≤ **50 行**，需在 commit message 中标注 `refactor:`。
    - **类型定义 / 接口 / 配置文件**：不计入行数限制，但须独立提交，不得与实现代码混提。
3.  **主动展示 Diffs (Review)**：AI 必须主动展示 `git diff` 或代码快照供人类评审。
4.  **原子验证 (Verify)**：运行 `tsc`、`lint` 或 `npm test`。展示通过结果。
5.  **等待回复 `1`**：**严禁**在未获收 `1` 之前调用 `git commit` 或开启下一个原子项。

## 🏗️ 2. 编码准则 (SOLID/DRY)
- 前端：使用极致设计感（视觉动态、和谐配色）。
- 逻辑：业务逻辑必须抽离到服务层或 Hooks，严禁在渲染层堆叠算法。
- 零占位符：禁止任何形式的 `TODO`。

## 🔱 3. Commit 协议
- **格式**：严格遵循 Conventional Commits (feat/fix/chore)。
- **逻辑隔离**：一原子一提交。禁止在同一个提交中混合 UI 改动与业务逻辑。
- **冲突熔断**：遇 Git 冲突立即停止，禁止 AI 私自胡搞。

## ⌨️ 指令：
- `/e` (执行) | `/v` (验证) | `/gc` (物理提交) | `/g` (全量同步)
- `/cp` (进度汇总)
